

#include <string>
#include <iostream>

// To simplify the use of std
using namespace std;

int main()
{
    // Define
    std::string word;

    // Reading
    std::cout << "Type a word: ";
    std::cin >> word;
    std::cout << "You typed: " << word;

    string country;

    cout << "\nType a country: ";
    cin >> country;
    cout << "You typed country: " << country;
}